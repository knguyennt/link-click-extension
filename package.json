{
  "name": "my-extension",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "dev": "vite build --watch",
    "dev:frontend": "vite build --watch",
    "dev:extension": "web-ext run --reload --source-dir dist/extension --target chromium"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "web-ext": "^8.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "@vue/tsconfig": "^0.7.0",
    "concurrently": "^9.1.2",
    "typescript": "~5.7.2",
    "vite": "^6.1.0",
    "vite-plugin-static-copy": "^2.2.0",
    "vue-tsc": "^2.2.0"
  }
}
